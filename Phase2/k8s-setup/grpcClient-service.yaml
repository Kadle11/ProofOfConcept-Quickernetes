apiVersion: v1
kind: Service
metadata:
  name: grpc-client
  labels:
    name: grpc-client
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 31500
      name: grpc-client
  selector:
    name: grpc-client
